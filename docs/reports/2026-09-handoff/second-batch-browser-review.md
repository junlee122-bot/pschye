# 첫 연결 묶음 브라우저 검증 기록

- 검증일: 2026-09-12
- 대상 커밋: `c74e47d0efadd0d420c8ea1b1044e7f14cf1c32c`
- 빌드 시작 당시 작업 트리 clean. 기존 `tmp/first-batch-review/build.mjs`로 실제 App 포함 검증 페이지를 빌드했다. Vite 6.4.3 빌드 성공(1분 22초). 대용량 청크 경고 있음.
- 격리 주소: `http://127.0.0.1:4395/tmp/first-batch-review/index.html`
- 브라우저: Edge, CUA. 실제 사용자 저장이 있는 다른 origin은 접근하지 않았다.
- 이후 다른 담당자가 수정한 마을 상호작용 및 플레이어/작가 기록 분리는 이 빌드에 포함되지 않는다.

## 실제 UI에서 확인한 내용

1. 검증 버튼으로 DAY 42 캠페인을 저장하고 IndexedDB 백업 후 localStorage 슬롯을 손상시켰다. 실제 앱이 DAY 42로 표시되었다. 이어 저장 상태 읽기의 결과는 local `ready / day 42`, backup `found / day 42`였다.
2. 기본 4인 캠페인(라온·카인·레오·크리스)으로 첫 임무의 연민 선택 → 출전 → 액션 모드 → 안내창까지 진입했다. HP `139/139`, 방어 `12`가 검증 준비값과 일치했다.
3. 동료 명령은 카인 결투 표식, 레오 진훤류 수문, 크리스 측면 유도 3개만 슬롯 `1~3`으로 표시되었다.
4. 연민 선택의 목표 방벽 +24와 보호 교리를 반영한 초기 호송 마차 `135/135`, 방벽 `42`가 표시되었다. 시작 사기 0, 전쟁 압박 100%, 현장 신뢰 5가 안내와 일치했다.
5. 첫 임무 규칙 `24초 = 1라운드`, 제한 `144초 / 6라운드`가 실제 UI에 표시되었다.
6. 액션 안내창을 390×844 뷰포트에서 관측했다. 안내 내용과 시작 버튼은 보였으나 카드의 오른쪽 테두리가 화면 밖으로 잘린 것으로 보였다. 뷰포트는 검증 후 복원했다.
7. 해당 시점 브라우저 warn/error 로그 조회는 빈 배열이었다. 이후 모든 실행 중 오류가 없었다는 보장은 아니다.

## 발견 사항

- 모바일 안내 카드: `src/styles.css:11003`의 바깥 컨테이너 좌우 padding 22px과 `:11017`의 `width:min(760px,96vw)`가 함께 적용된다. 폭 390px에서는 내부 가용폭 346px보다 카드 지정폭 374.4px가 크다. 실제 스크린샷의 오른쪽 잘림과 일치한다. DOM 경계 측정은 CUA 시간초과로 실패했으므로 측정값으로 확정하지 않는다.
- 기본 4인 레오 명령의 접근성 도움말은 실제 적용 수치 `목표 방벽 +40` 뒤에 원문 설명의 `36의 방벽`을 함께 표시했다. `src/components/RaonActionBattle.tsx:780`에서 성장 적용 수치와 고정 설명을 연결하는 부분이다. 동작 오류는 확인하지 않았다.

## 브라우저로 확인하지 못한 내용

- 성장한 4인 및 6인 준비값과 실제 액션 화면의 비교
- 액션/전술 모드 전환 확인·취소와 재시작 동작
- 안내창을 닫은 실제 전투 중 이동/명령/패링/승패
- 전투 중 모바일 이동 패드·명령·동료 명령의 겹침
- 현재 작업 트리의 마을 상호작용과 새 기록 접근 제어

새 동작을 확인하던 중 `Input.dispatchMouseEvent` CDP 시간초과가 두 번 발생했다. 두 클릭은 실제로 처리되어 다음 AX 읽기로 진행 화면을 확인할 수 있었다. 이후 읽기 전용 DOM 측정도 `Page.createIsolatedWorld` 시간초과로 실패하여 추가 UI 조작을 중단했다. 모바일 스크린샷은 도구 결과에 표시되었지만 파일로 저장하려던 캡처가 `Page.captureScreenshot` 시간초과로 실패했다. 저장된 스크린샷 파일은 없다.

## 코드로만 확인한 연결 경로

- `src/game/actionBattleRules.ts:44`: 입력 HeroDefinition을 중복 제거 후 그대로 받아 Raon 및 기술 스탯을 사용한다. `:57`에서 라온 외 편성 전원을 동료 명령으로 만든다. `:60`에서 해당 임무의 진실 발동 기술을 선택한다. 이 구조는 6인/성장 연결을 지원하지만 이번 브라우저 검증으로 동작을 확정한 것은 아니다.
- `src/components/RaonActionBattle.tsx:615`: 출전 당시 모델을 고정하고 `:777`에서 동료 전체를 명령 버튼으로 렌더링한다.
- `src/components/BattleScreen.tsx:222`: 모드 전환은 확인창 승인 후 restart를 호출하며, `:211`에서 전투 상태·기록·선택·모바일 콘솔·통지를 초기화한다. 브라우저에서 승인/취소 경로를 실행하지 않았다.

본 검증에서는 제품 소스 수정, 새 테스트 실행, 커밋, 배포를 하지 않았다.

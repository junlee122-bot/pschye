# 최초 수정 전 .gitignore 기록

원본 백업 ZIP에 남은 초기 제외 규칙입니다. 현재 설정으로 적용하는 파일이 아니라 역사 기록입니다. 원본은 CRLF이며 줄바꿈을 LF로 정규화한 Git blob은 기존 Git 이력에 존재합니다. 민감 값 없이 파일·폴더 제외 패턴만 포함합니다.

```gitignore
node_modules/
dist/
.vite/
coverage/
*.log
.DS_Store
Thumbs.db

.vercel
.env.3d.local
.tools/
```
